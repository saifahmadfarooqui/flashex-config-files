server:
  port: 4200
# eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: .

# eureka: 
#   client: 
#     register-with-eureka: true
#     fetch-registry: true
